# Agent Instructions

## Branching and Stacking

- Use the Graphite CLI (`gt`) instead of `git` for branch creation, branch switching, stacked changes, stack synchronization, and stack submission.
- Use `git` only for operations that `gt` does not cover, such as inspecting repository state or diffs.
